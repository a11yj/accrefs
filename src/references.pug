extends /base.pug

block main
  .Stack.-large
    h1.Heading=title

    .Stack.-large
      each reference, index in collections.references.slice().reverse()
        article.Stack.-small(id=reference.fileSlug)
          h2.ItemTitle
            a.WithIcon.-reference(href=reference.data.link)=reference.data.title
          !=reference.templateContent
          p
            span.WithIcon.-tag
              each tag, index in reference.data.tags
                if index
                  | ・
                a(href=url(`/tags/${tags.find(t => t.title === tag).slug}/`))=tag
