extends /base.pug

block main
  h1.VisuallyHidden=`${site.title} ホーム`

  .Stack.-large
    section#tags(aria-labelledby="tags-heading")
      .Stack.-large
        h2#tags-heading.Heading タグ

        .Stack.-large
          each tag, index in tags
            article.Stack.-small(id=`tags:${tag.slug}`)
              h3.ItemTitle
                a.WithIcon.-tag(href=url(`/tags/${tag.slug}/`))=tag.title
              if tag.content
                p=tag.content

        div
          a.WithIcon.-reference(href=url('/references/')) すべての参考資料

    .Divider

    section#contribution(aria-labelledby="contribution-heading")
      .Stack
        h2#contribution-heading.Heading ウェブサイトに貢献

        p このウェブサイトではあなたからの情報提供をお待ちしています。参考資料の追加や修正のリクエストは次のいずれかの方法で行えます。

        ul
          li
            a(href='https://github.com/a11yj/accrefs/') GitHubからプルリクエストを送る
          li Googleフォームから送信する（準備中）
          li A11YJのSlackで発言する（準備中）
